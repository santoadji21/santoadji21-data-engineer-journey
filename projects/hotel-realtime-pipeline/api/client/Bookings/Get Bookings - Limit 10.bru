meta {
  name: Get Bookings - Limit 10
  type: http
  seq: 2
}

get {
  url: {{base_url}}/bookings?limit=10
  body: none
  auth: none
}

docs {
  # Get Bookings - Limit 10
  
  Same as default, but only returns the 10 most recent bookings.

  Useful for quick testing or previewing the data structure.
}

meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

docs {
  # Health Check
  Verify the API is running and the data file exists.

  **Expected Response:**
  ```json
  {
    "status": "healthy",
    "data_file_exists": true
  }
  ```
}
